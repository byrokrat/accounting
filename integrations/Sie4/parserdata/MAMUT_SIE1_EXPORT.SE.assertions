<?php

use byrokrat\accounting\Container;

return function (Container $container) {
    $account = $container->select()->account('1512');

    $this->assertSame(
        "Belånade fordringar",
        $account->getDescription()
    );

    $this->assertSame(
        '0',
        $account->getAttribute('sru')
    );
};
